# Pinned from the working .venv (Python 3.11) for reproducible builds.
# Regenerate after upgrades with:  pip freeze > requirements.lock.txt

# ── Core LLM + Agent ──────────────────────────
langchain==1.3.11
langchain-openai==1.3.3
langgraph==1.2.6
openai==2.44.0

# ── Vector DB + Sparse ────────────────────────
pinecone==9.1.0
pinecone-text==0.11.0

# ── Web framework ─────────────────────────────
fastapi==0.138.1
uvicorn==0.49.0
python-multipart==0.0.32   # required by FastAPI for UploadFile/Form (/transcribe)
pydantic==2.13.4

# ── Voice I/O ─────────────────────────────────
elevenlabs==2.54.0         # primary TTS
gTTS==2.2.4                # TTS fallback (no API key needed)
librosa==0.11.0
soundfile==0.14.0
av==18.0.0                 # audio decoding in voice_emotion.py (was an undeclared dependency)

# ── Privacy / PII scrubbing ───────────────────
presidio-analyzer==2.2.363
presidio-anonymizer==2.2.363
spacy==3.8.13
# Also required (spaCy model, not pip-installable by name):
#   python -m spacy download en_core_web_lg

# ── Emotion detection (webcam) ────────────────
opencv-python-headless==4.13.0.92
pillow==12.2.0
deepface==0.0.100
tf-keras==2.21.0
numpy==2.4.6

# ── Utilities ─────────────────────────────────
python-dotenv==1.2.2
tqdm==4.68.3
datasets==3.6.0            # only needed for ingest_to_pinecone.py
requests==2.34.2           # only needed for ingest_to_pinecone.py
tiktoken==0.13.0           # only needed for ingest_to_pinecone.py (token-aware batching)
